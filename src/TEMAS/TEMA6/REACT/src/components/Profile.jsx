function Profile({ name, age, profession }) {
    return (
        <div>
            <p>Nombre: {name}</p>
            <p>Edad: {age}</p>
            <p>Profesión: {profession}</p>
        </div>
    );
}

export default Profile;
